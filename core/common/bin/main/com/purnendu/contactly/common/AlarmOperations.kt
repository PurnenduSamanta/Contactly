package com.purnendu.contactly.common

/**
 * Constants for alarm operations.
 * Shared across :core:data (ActivationsRepository), :platform:alarm (receivers), etc.
 */
object AlarmOperations {
    const val OP_APPLY = "apply"
    const val OP_REVERT = "revert"
}
